# Dependencies for the nightly agents run (orchestrator + all sub-agents).
# Deliberately excludes: fastapi, uvicorn, celery, redis, alembic, telegram.

# Agent orchestration
langgraph>=0.1.0
langchain>=0.2.0
langchain-anthropic>=0.1.0
langchain-core>=0.2.0
anthropic>=0.28.0

# Market data & technical analysis
yfinance>=0.2.38
pandas>=2.2.0
numpy>=1.26.0

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0

# Validation & config
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# HTTP client (used by LangChain internals)
httpx>=0.27.0
